<Window x:Class="Pomodoro.StatsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Focus stats"
        Width="470" SizeToContent="Height"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#2B2B2B"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#DDDDDD"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="GroupTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,12,0,6"/>
        </Style>
    </Window.Resources>

    <StackPanel Margin="16">
        <TextBlock x:Name="SummaryText" FontSize="13"/>

        <TextBlock Text="WHEN YOU FOCUS (DAY × HOUR)" Style="{StaticResource GroupTitle}"/>
        <Grid x:Name="HeatmapGrid"/>

        <TextBlock Text="Each cell = pomodoros completed in that hour of the week."
                   Foreground="#777777" FontSize="10" TextWrapping="Wrap" Margin="0,10,0,0"/>
    </StackPanel>
</Window>
